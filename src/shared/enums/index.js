const TICKET_STATUS = require('./ticket-status.enum');
const WORKLOG_TYPE = require('./worklog-type.enum');
const ERROR_TYPE = require('./error-type.enum');
const SEVERITY = require('./severity.enum');
const ENVIRONMENT = require('./environment.enum');
const USER_ROLE = require('./user-role.enum');

module.exports = {
    TICKET_STATUS,
    WORKLOG_TYPE,
    ERROR_TYPE,
    SEVERITY,
    ENVIRONMENT,
    USER_ROLE,
};